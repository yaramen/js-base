// Три разных способа создать литерал строки
'строка';
"тоже строка";
`еще одна стока ${'!'}`;

// Присвоение строки
const js = 'JavaScript'
console.log(js)

const ts = 'TypeScript';

// конкатенация строк
console.log(js + ' ' + ts)
// строковая интерполяция
console.log(`${js} ${ts}`)
